export function MaxOrigemLogo({ className = "" }: { className?: string }) {
  return (
    <img
      src="/brand/max-origem.png"
      alt="MAX Origem"
      width={1677}
      height={606}
      className={`h-12 w-auto max-w-[220px] bg-transparent object-contain object-left ${className}`}
    />
  );
}
